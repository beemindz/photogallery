<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
      <item name="actionBarStyle">@style/MyActionBar</item>

      <!--<item name="actionMenuTextAppearance">@style/MyActionBar.MenuTextStyle</item>-->
      <item name="actionOverflowButtonStyle">@style/OverFlow</item>
      <item name="popupMenuStyle">@style/PopupMenu.Custom_actionbar</item>

    </style>

  <!-- ActionBar styles -->
  <style name="MyActionBar" parent="Widget.AppCompat.Light.ActionBar">
    <!-- Support library compatibility -->
    <item name="background">@android:color/white</item>
    <item name="actionMenuTextColor">@android:color/darker_gray</item>
    <item name="android:textColor">@android:color/white</item>
  </style>

  <style name="MyActionBar.OverFlow" parent="Widget.AppCompat.ActionButton.Overflow">
    <item name="android:src">@drawable/icon_sort</item>
  </style>

  <style name="PopupMenu.Custom_actionbar" parent="@style/Widget.AppCompat.Light.PopupMenu">
    <item name="android:popupBackground">#669900</item>

  </style>

</resources>
